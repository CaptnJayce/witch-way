package game 

import rl "vendor:raylib"
import "core:fmt"

Krushem :: struct {
    size: rl.Rectangle,
    texture: rl.Texture2D,
}

Rock :: struct {
    size: rl.Rectangle,
    texture: rl.Texture2D,
}
